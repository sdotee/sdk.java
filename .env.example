# SEE Java SDK Environment Variables
# Copy this file to .env.local and fill in your values
# Note: .env.local should be added to .gitignore to prevent committing sensitive data

# Required: Your SEE API authentication key
SEE_API_KEY=your-api-key-here

# Optional: API base URL (defaults to https://s.ee/api)
# SEE_API_BASE_URL=https://s.ee/api

# Optional: Java runtime options for Docker (adjust memory settings if needed)
# JAVA_OPTS=-Xmx256m -Xms128m

# Optional: Test domain for shortened URLs (defaults to a.see-test.com)
# SEE_TEST_DOMAIN=a.see-test.com

# Optional: Request timeout in seconds (defaults to 10)
# SEE_TEST_TIMEOUT=10

# Docker Registry Configuration (for pushing images)
# DOCKER_REGISTRY=docker.io
# DOCKER_USER=yourusername
