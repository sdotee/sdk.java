# SEE Java SDK Test Configuration
# Copy this file to .env and fill in your actual values
# Note: .env should be added to .gitignore to prevent committing sensitive data

# Required: Your SEE API authentication key
SEE_API_KEY=your-api-key-here

# Optional: API base URL (defaults to https://api.see-test.com/api)
# SEE_API_BASE_URL=https://api.see-test.com/api

# Optional: Test domain for shortened URLs (defaults to a.see-test.com)
# SEE_TEST_DOMAIN=a.see-test.com

# Optional: Request timeout in seconds (defaults to 10)
# SEE_TEST_TIMEOUT=10
